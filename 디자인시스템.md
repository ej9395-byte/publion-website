# 퍼블리온 홈페이지 디자인 시스템

기준일 2026-09-02 · 구현 코드에서 그대로 옮긴 값입니다
출처 `assets/css/styles.css` · `assets/js/views.js` · Publion Design SOP v1.0

이 문서는 시안이 아니라 **현재 돌아가는 사이트의 실제 값**을 적은 것입니다.
값을 고칠 때는 CSS를 먼저 고치고 이 문서를 맞추면 됩니다. 반대로 하면 어긋납니다.

---

## 1. 이 디자인이 지키려는 것

책 표지가 화면에서 가장 강한 색이어야 합니다. 나머지는 전부 물러섭니다.
흰 바탕, 검은 글자, 가는 괘선, 강조색 하나. 그게 전부입니다.

그래서 하지 않는 것이 분명합니다.

- 둥근 모서리를 쓰지 않습니다
- 그림자를 깔지 않습니다
- 그라데이션을 쓰지 않습니다
- 느낌표를 쓰지 않습니다
- 이모지를 쓰지 않습니다
- "혁신적인" "특별한" 같은 빈 수식어를 쓰지 않습니다

예외는 두 곳뿐이고, 아래 8장에 이유와 함께 적어 두었습니다.

---

## 2. 색

토큰 이름 그대로 CSS 변수로 들어 있습니다. 색을 직접 적지 말고 변수를 부르세요.

### 흰 바탕 위

| 변수 | 값 | 쓰는 곳 |
|---|---|---|
| `--c-bg` | `#FFFFFF` | 바탕 |
| `--c-ink` | `#111111` | 제목·기본 글자 |
| `--c-body` | `#3F3F3D` | 본문 |
| `--c-muted` | `#6B6B68` | 부제·보조 설명 |
| `--c-faint` | `#8C8C88` | 입력창 안내 글자 |
| `--c-accent` | `#9E2B1F` | 강조 하나. 링크 hover·분류 라벨 |
| `--c-rule` | `#E2E2DE` | 괘선 |
| `--c-gray` | `#F4F4F2` | 연회색 면 |
| `--c-imgbg` | `#EEEEEC` | 이미지 자리 바탕 |

### 먹 위 (히어로·푸터처럼 어두운 면)

| 변수 | 값 | 쓰는 곳 |
|---|---|---|
| `--c-on-ink` | `#B4B4AE` | 어두운 면 위 보조 글자 |
| `--c-rule-ink` | `#2A2A28` | 어두운 면 위 괘선 |
| `--c-line-ink` | `#3A3A38` | 어두운 면 위 입력 밑줄 |
| `--c-accent-lt` | `#D98B72` | 어두운 면 위 강조 |

강조색은 `#9E2B1F` 하나입니다. 어두운 면에서만 밝은 쪽 `#D98B72`로 바뀝니다.
강조를 두 번째로 늘리고 싶어지면, 그건 대개 위계를 잘못 잡은 신호입니다.

---

## 3. 서체

2026-09-02에 제목용 한글을 명조에서 고딕으로 바꿨습니다.

```css
--f-title: 'Noto Sans KR', 'Apple SD Gothic Neo', 'Malgun Gothic', sans-serif;
--f-sans:  'Archivo', 'Noto Sans KR', 'Apple SD Gothic Neo', system-ui, sans-serif;
```

굵기는 Noto Sans KR 300·400·500·700, Archivo 400·500·600을 불러옵니다.
구글 폰트 한 줄로 받아오고, 링크는 `build/prerender.mjs`에 있습니다.

### 한글은 곧게, 영문만 기울입니다

Noto Sans KR에는 이탤릭 자획이 아예 없습니다. 그냥 `font-style: italic`을 걸면
브라우저가 글자를 억지로 기울여 버립니다. 그래서 이탤릭이 걸린 네 곳 전부에
`font-synthesis-style: none`을 함께 넣었습니다. 결과는 이렇습니다.

> *AI* 시대, 인간에게만 허락된 것 — 영문 AI만 기울고 한글은 섭니다

### 크기 단계

| 유틸 | 크기 | 굵기 | 쓰는 곳 |
|---|---|---|---|
| `.t-h1-page` | 52px | 700 · 자간 -0.02em | 페이지 큰 제목 |
| `.t-h2` | 30px | 600 | 구역 제목 |
| `.t-en` | 상속 | 400 이탤릭 | 영문 부제 |
| `.t-label` | 11px | 자간 0.14em · 대문자 | 분류 라벨·날짜 |
| `.t-crumb` | 11px | 자간 0.16em · 대문자 | 현재 위치 표시 |

큰 제목의 자간을 살짝 좁힌 이유가 있습니다. 고딕은 명조보다 획이 굵어 보여서,
52px에서 글자가 서로 밀어내는 느낌이 납니다. -0.02em이 그걸 잡아 줍니다.

---

## 4. 여백과 골격

```css
--pad-x:    32px;   /* 좌우 여백 */
--sec-y:    78px;   /* 구역 사이 세로 여백 */
--hero-h:   620px;  /* 히어로 높이 */
--header-h: 112px;  /* 머리말 높이 */
```

머리말은 두 줄입니다. 위가 로고, 아래가 메뉴입니다. 합쳐서 112px입니다.
스크롤해도 따라오지 않습니다. 책을 보는 화면에서 머리말이 따라다니면
표지가 잘립니다.

---

## 5. 화면 폭에 따른 변화

분기점은 네 개입니다.

| 폭 | 무엇이 달라지나 |
|---|---|
| 1100px 이하 | 격자 열 수가 줄어듭니다 |
| 820px 이하 | 메뉴가 접힙니다. 히어로가 세로로 쌓입니다 |
| 520px 이하 | 한 열이 됩니다. 글자 단계가 한 칸 내려갑니다 |
| 360px 이하 | 좌우 여백을 더 줄여 표지 폭을 지킵니다 |

521px에서 820px 사이에는 3열 격자를 따로 두었습니다. 태블릿 세로에서
2열은 허전하고 4열은 표지가 너무 작아지기 때문입니다.

`prefers-reduced-motion` 분기도 있습니다. 움직임을 줄여 달라는 설정을 켠
사람에게는 등장 애니메이션이 아예 실행되지 않습니다. 이때 카드가 투명한 채로
남지 않도록 최종 상태를 따로 지정해 두었습니다.

---

## 6. 되풀이되는 조각들

| 조각 | 생김새 |
|---|---|
| 책 카드 | 표지 · 분류 라벨 · 제목 · 저자 · 정가. 테두리 없이 여백으로 구분 |
| 구역 머리 | 한글 제목 + 영문 이탤릭 부제를 나란히. 오른쪽 끝에 "전체 보기" |
| 버튼 | 먹 바탕에 흰 글자, 모서리 각짐. hover에서 강조색 |
| 괘선 | 1px `--c-rule`. 두 줄 이상 겹치지 않게 |
| 분류 라벨 | 11px 대문자 자간 넓힘. 강조색 또는 보조색 |

---

## 7. 접근성에서 지킨 것

- 디자인은 `div`에 클릭을 걸지만 구현은 `button`과 `a`를 씁니다. 보이는 결과는
  같고 키보드로 다닐 수 있습니다
- 포커스가 가면 강조색 2px 외곽선이 뜹니다. 안쪽으로 파고들지 않게 2px 띄웁니다
- 화면에 안 보이고 낭독기에만 읽히는 글자는 `.u-sr-only`로 처리합니다
- 현재 페이지 메뉴에 `aria-current="page"`를 붙입니다. 도서와 신간이 같은 경로를
  공유하므로 **첫 번째로 맞는 항목에만** 붙습니다

---

## 8. SOP를 어긴 두 곳

정직하게 적어 둡니다. 둘 다 의도한 예외입니다.

**메가 메뉴의 그림자** (`styles.css:261`)
`box-shadow: 0 26px 44px -32px rgba(17,17,17,.45)`
펼쳐진 메뉴가 아래 내용 위에 떠 있다는 걸 알려 줄 방법이 필요했습니다.
괘선만으로는 메뉴가 본문에 섞여 보입니다. 아주 넓게 퍼뜨려 그림자라기보다
경계가 흐려지는 정도로만 씁니다.

**저자 사진의 원형** (`styles.css:997`)
`border-radius: 50%`
사람 얼굴은 원형이 관습입니다. 여기서만 각을 고집하면 오히려 눈에 걸립니다.

이 둘 말고 둥근 모서리·그림자·그라데이션은 사이트 전체에 없습니다.

---

## 9. 현재 규모

| 항목 | 수 |
|---|---|
| 생성 페이지 | 46개 |
| 도서 상세 | 41개 |
| 스타일시트 | 1,711줄 |
| 화면 템플릿 | 825줄 |

페이지는 `node build/prerender.mjs` 한 번으로 다시 만들어집니다.
자바스크립트를 실행하지 않는 크롤러도 본문을 그대로 읽습니다.

---

## 10. 고칠 때

1. 색과 서체는 `styles.css` 맨 위 `:root` 블록만 고칩니다
2. 화면 구조는 `assets/js/views.js`를 고칩니다. 브라우저와 빌드가 같은 함수를 씁니다
3. 고친 뒤 `node build/prerender.mjs`를 돌립니다
4. 1440px과 375px 두 폭에서 눈으로 확인합니다

`views.js`에는 서체를 직접 적은 곳이 한 군데도 없습니다. 전부 변수를 거칩니다.
그래서 서체 교체가 변수 두 줄로 끝났습니다. 이 상태를 유지하는 게 좋습니다.

---

## SOP와의 관계

Claude Design의 `Publion Design SOP.dc.html`을 v1.1로 올려 고딕 기준에 맞췄습니다
(2026-09-02). 루트와 `publion/` 두 사본 모두 갱신했고, 3장 서체 표의 굵기와
자간도 이 문서와 같은 값입니다. 로컬 사본은 `design-source/`에 있습니다.

v1.2에서 10장 "남은 항목"도 실제 상태로 다시 셌습니다. 끝난 것 7건과 남은 것
6건을 표로 나눴고, 공개 전에 반드시 처리해야 할 두 가지를 따로 상자로 뽑았습니다.
뉴스레터가 아무것도 수집하지 않는 것과, 편집자·대표의 말 41편이 검수를 안 거친
것입니다.
